# 02 Design-System Extraction

1. Extract the effective baseline tokens before introducing new visual values.
2. Alias background, surfaces, border, text, accent, status, spacing, radius, elevation, typography, and density.
3. Define Loading, Empty, Error, Low-confidence, and Offline states.
4. Preserve the navy/cyan identity and avoid decorative re-skinning.
5. Gate: `docs/DESIGN_TOKENS.md` exists and V2.1 CSS uses semantic aliases.

{
  "name": "china-industry-atlas-2026",
  "version": "1.0.0",
  "private": true,
  "description": "A city-first, one-page atlas for researching China's regional industries and representative enterprises.",
  "license": "PolyForm-Noncommercial-1.0.0",
  "scripts": {
    "check": "node --check assets/data.js && node --check assets/generated/a-share-enterprises.js && node --check assets/generated/neeq-enterprises.js && node --check assets/generated/twse-enterprises.js && node --check assets/generated/curated-city-enterprises.js && node --check assets/generated/city-profiles.js && node --check assets/app.js && node --check assets/v21-app.js && node --check assets/v22-app.js && node tests/smoke.mjs",
    "generate:profiles": "node scripts/generate-city-profiles.mjs",
    "import:tw-map": "node scripts/import-taiwan-map.mjs",
    "test:smoke": "npm run check",
    "test:browser": "playwright test",
    "test": "npm run check && npm run test:browser",
    "serve": "python3 -m http.server 4175"
  },
  "keywords": [
    "china",
    "industry-atlas",
    "city-research",
    "enterprise-intelligence",
    "regional-economy",
    "产业图谱",
    "城市尽调"
  ],
  "devDependencies": {
    "@playwright/test": "1.54.2"
  }
}
